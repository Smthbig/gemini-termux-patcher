diff --git a/packages/cli/src/config/settings.ts b/packages/cli/src/config/settings.ts
index b84c1bda4..ec839c7fd 100644
--- a/packages/cli/src/config/settings.ts
+++ b/packages/cli/src/config/settings.ts
@@ -108,6 +108,8 @@ export function getSystemSettingsPath(): string {
     return '/Library/Application Support/GeminiCli/settings.json';
   } else if (platform() === 'win32') {
     return 'C:\\ProgramData\\gemini-cli\\settings.json';
+  } else if (platform() === 'android') {
+    return '/data/data/com.termux/files/usr/etc/gemini-cli/settings.json';
   } else {
     return '/etc/gemini-cli/settings.json';
   }
@@ -226,7 +228,9 @@ function setNestedProperty(
     if (typeof next === 'object' && next !== null) {
       // eslint-disable-next-line @typescript-eslint/no-unsafe-type-assertion
       current = next as Record<string, unknown>;
-    } else {
+    } else if (platform() === 'android') {
+    return '/data/data/com.termux/files/usr/etc/gemini-cli/settings.json';
+  } else {
       // This path is invalid, so we stop.
       return;
     }
@@ -871,7 +875,9 @@ export function migrateDeprecatedSettings(
           delete settings[oldKey];
           modified = true;
         }
-      } else {
+      } else if (platform() === 'android') {
+    return '/data/data/com.termux/files/usr/etc/gemini-cli/settings.json';
+  } else {
         // Only old exists, migrate to new (inverted)
         settings[newKey] = !oldValue;
         if (removeDeprecated) {
diff --git a/packages/core/src/config/storage.ts b/packages/core/src/config/storage.ts
index 5a40648a4..e419cfb9d 100644
--- a/packages/core/src/config/storage.ts
+++ b/packages/core/src/config/storage.ts
@@ -139,6 +139,8 @@ export class Storage {
       return '/Library/Application Support/GeminiCli';
     } else if (os.platform() === 'win32') {
       return 'C:\\ProgramData\\gemini-cli';
+    } else if (os.platform() === 'android') {
+      return '/data/data/com.termux/files/usr/etc/gemini-cli';
     } else {
       return '/etc/gemini-cli';
     }
diff --git a/packages/core/src/utils/secure-browser-launcher.ts b/packages/core/src/utils/secure-browser-launcher.ts
index 4b8017b18..a860bd2fa 100644
--- a/packages/core/src/utils/secure-browser-launcher.ts
+++ b/packages/core/src/utils/secure-browser-launcher.ts
@@ -80,6 +80,7 @@ export async function openBrowserSecurely(url: string): Promise<void> {
       ];
       break;
 
+    case 'android':
     case 'linux':
     case 'freebsd':
     case 'openbsd':
@@ -170,7 +171,7 @@ export function shouldLaunchBrowser(): boolean {
   const isSSH = !!process.env['SSH_CONNECTION'];
 
   // On Linux, the presence of a display server is a strong indicator of a GUI.
-  if (platform() === 'linux') {
+  if (platform() === 'linux' || platform() === 'android') {
     // These are environment variables that can indicate a running compositor on Linux.
     const displayVariables = ['DISPLAY', 'WAYLAND_DISPLAY', 'MIR_SOCKET'];
     const hasDisplay = displayVariables.some((v) => !!process.env[v]);
diff --git a/packages/core/src/utils/shell-utils.ts b/packages/core/src/utils/shell-utils.ts
index a14b28227..ff0c75139 100644
--- a/packages/core/src/utils/shell-utils.ts
+++ b/packages/core/src/utils/shell-utils.ts
@@ -672,7 +672,7 @@ export function getShellConfiguration(): ShellConfiguration {
   }
 
   // Unix-like systems (Linux, macOS)
-  return { executable: 'bash', argsPrefix: ['-c'], shell: 'bash' };
+  return { executable: process.env['TERMUX_VERSION'] ? 'sh' : 'bash', argsPrefix: ['-c'], shell: 'bash' };
 }
 
 /**
